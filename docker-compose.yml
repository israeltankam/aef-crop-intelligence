version: "3.8"
services:
  web:
    build: .
    volumes:
      - ./:/app:cached
    ports:
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
    command: >
      sh -c "streamlit run app.py --server.port 8501 --server.address 0.0.0.0 --server.enableCORS false"
